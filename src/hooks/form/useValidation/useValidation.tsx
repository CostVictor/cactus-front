import { PropsUseValidation } from "./usevalidation.types";
import { validations } from "./usevalidation.variables";

const useValidation = ({
  type,
  config,
  required,
}: PropsUseValidation) => {
  return;
};

export default useValidation;
